# ChemAssist

ChemAssist is a starter repository for building chemistry-assistant tooling.

## Initial structure

- `src/` for source code
- `.gitignore` with common defaults
- `LICENSE` (MIT)

## Next steps

1. `cd ChemAssist`
2. `git init`
3. `git add .`
4. `git commit -m "Initial commit"`
